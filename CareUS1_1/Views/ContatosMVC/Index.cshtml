@model IEnumerable<CareUS1_1.Models.Contato>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.fkIdCliente)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.telefone)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.nomeContato)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.fkIdCliente)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.telefone)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.nomeContato)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.GuidContato">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.GuidContato">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.GuidContato">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
